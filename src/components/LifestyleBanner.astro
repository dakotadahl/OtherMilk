<section class="banner" aria-label="Good milk, better weekly">
  <div class="banner__image-wrap">
    <picture>
      <source srcset="/images/lifestyle.webp" type="image/webp" />
      <img
        src="/images/lifestyle.jpg"
        alt="Other Milk bottles arranged in a crate"
        width="1318"
        height="671"
        loading="lazy"
        decoding="async"
      />
    </picture>
    <div class="banner__overlay">
      <p class="banner__text">Good<br />milk.</p>
      <p class="banner__text">Better<br />weekly.</p>
    </div>
  </div>
</section>

<style>
  /* Figma: Frame 16 is 1440×821, image at x=61 w=1318 → 61px padding each side */
  .banner {
    max-width: var(--page-max);
    margin: 0 auto;
    padding: clamp(40px, 5.21vw, 75px) clamp(20px, 4.24vw, 61px);
  }

  .banner__image-wrap {
    position: relative;
    max-width: 1318px;
    margin: 0 auto;
    border-radius: var(--radius-banner);
    -webkit-mask-image: -webkit-radial-gradient(white, black);
    mask-image: radial-gradient(white, black);
    overflow: hidden;
    box-shadow: var(--shadow-postcard);
    aspect-ratio: 1318 / 671;
  }

  .banner__image-wrap img {
    position: absolute;
    inset: -2% 0 0;
    width: 100%;
    height: 124%;
    object-fit: cover;
    will-change: transform;
  }

  .banner__overlay {
    position: absolute;
    inset: 0;
    display: flex;
    justify-content: space-between;
    align-items: center;
    /* Figma: "Good milk." at x=256, "Better weekly." right edge at x=1189
       On 1318-wide image that's roughly 195px and 129px inset */
    padding: 0 clamp(24px, 9.86vw, 130px) 0 clamp(24px, 14.79vw, 195px);
    pointer-events: none;
  }

  .banner__text {
    font-family: var(--font-display);
    font-size: clamp(18px, 4.14vw, 59.55px);
    text-transform: uppercase;
    color: var(--color-white);
    line-height: 1.05;
    text-align: center;
  }

  @media (max-width: 768px) {
    .banner__image-wrap {
      aspect-ratio: 3 / 4;
    }

    .banner__image-wrap img {
      height: 140%;
    }
  }

  @media (max-width: 480px) {
    .banner__overlay {
      flex-direction: column;
      justify-content: space-between;
      align-items: center;
      gap: 0;
      padding: clamp(24px, 6vw, 40px) 16px;
    }

    .banner__text {
      font-size: clamp(28px, 7vw, 36px);
    }
  }
</style>
