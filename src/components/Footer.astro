<footer class="footer">
  <div class="footer__wordmark">
    <img
      src="/images/footer-wordmark.svg"
      alt="Other Milk"
      width="1638"
      height="763"
      loading="lazy"
      decoding="async"
    />
  </div>

  <nav class="footer__links" aria-label="Footer navigation">
    <a href="/about">About Us</a>
    <a href="/products">Products</a>
    <a href="/wholesale">Wholesale</a>
    <a href="/find-us">Find us</a>
    <a href="#cart" class="subbly-cart">Cart</a>
    <a href="/subscribe">Subscribe</a>
    <a href="https://www.subbly.co/account/auth/login?store=drinkother" class="footer__login" target="_blank" rel="noopener noreferrer">Log in<span class="sr-only">(opens in new tab)</span></a>
  </nav>
</footer>

<style>
  .footer {
    padding: 0 0 50px;
    text-align: center;
    overflow: hidden;
  }

  /* Wordmark should always bleed off both edges of the viewport */
  .footer__wordmark {
    width: 113.75%;
    max-width: none;
    margin-left: -6.875%;
  }

  .footer__wordmark img {
    width: 100%;
    height: auto;
  }

  .footer__links {
    display: flex;
    justify-content: center;
    align-items: center;
    width: 100%;
    max-width: none;
    margin: 20px auto 0;
    gap: clamp(16px, 2.78vw, 40px);
    flex-wrap: nowrap;
    padding: 0 20px;
  }

  .footer__links a {
    position: relative;
    font-family: var(--font-body);
    font-weight: 700;
    font-size: clamp(12px, 1.11vw, 16px);
    letter-spacing: clamp(2px, 0.33vw, 4.8px);
    text-transform: uppercase;
    color: var(--color-pop-accessible);
    font-feature-settings: 'dlig';
    transition: opacity 0.2s;
  }

  .footer__links a::after {
    content: '';
    position: absolute;
    left: 0;
    bottom: -2px;
    width: 100%;
    height: 1.5px;
    background: currentColor;
    transform: scaleX(0);
    transform-origin: right;
    transition: transform 0.3s ease;
  }

  .footer__links a:hover::after {
    transform: scaleX(1);
    transform-origin: left;
  }

  .footer__links a:hover {
    opacity: 0.7;
  }

  .footer__login {
    color: var(--color-muted);
  }

  /* Override Subbly widget styles on cart link */
  .footer__links .subbly-cart {
    font-family: var(--font-body);
    font-weight: 700;
    font-size: clamp(12px, 1.11vw, 16px);
    letter-spacing: clamp(2px, 0.33vw, 4.8px);
    text-transform: uppercase;
  }

  @media (max-width: 480px) {
    .footer {
      padding-bottom: 32px;
    }

    .footer__links {
      flex-wrap: wrap;
      gap: 12px 20px;
    }
  }
</style>
